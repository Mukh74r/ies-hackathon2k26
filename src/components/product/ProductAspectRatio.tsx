import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

/**
 * ProductAspectRatio component to maintain consistent dimensions 
 * for media assets (images, videos, etc).
 */
const ProductAspectRatio = AspectRatioPrimitive.Root;

export { ProductAspectRatio as AspectRatio };
